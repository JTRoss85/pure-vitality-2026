# Cloudflare Pages redirects
# https://developers.cloudflare.com/pages/configuration/redirects/
#
# These are the REAL URLs from the live Squarespace site, confirmed by crawling
# https://www.purevitalityinfusions.com on 2026-08-26. Seven of its nine slugs
# (/about, /contact, /faq, /iv-therapy, /lab-draw, /mobile-infusions,
# /red-light-therapy, /wellness-injections) map 1:1 onto pages in this repo and
# resolve on their own, so they need no rule here.
#
# Only the paths whose destination actually CHANGED are listed below.

# "Services" was its own Squarespace page; it is now a homepage section.
/services         /#services      301

# Squarespace booking page — booking now runs through Vagaro, linked from
# every "Book a visit" button, so send this to Contact rather than off-site.
/appointments     /contact        301

# Squarespace Commerce cart, which this site does not have.
/cart             /contact        301
